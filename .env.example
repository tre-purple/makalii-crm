# Supabase — used for all CRM data (contacts CRUD, realtime, intake)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key

# Production API — used for authentication only for now (login/logout/session)
REACT_APP_API_URL=http://localhost:3000
